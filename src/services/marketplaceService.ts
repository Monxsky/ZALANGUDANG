// src/services/marketplaceService.ts
export const syncStockToMarketplaces = async (productId: number, stock: number) => {
  console.log(`[Dummy] Sync product ${productId} with stock ${stock} to marketplaces`);
};
